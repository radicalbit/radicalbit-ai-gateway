{{- if or (not .Values.postgresql.auth.username.existingSecret) (not .Values.postgresql.auth.password.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "common.fullname" . }}-psql-auth
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if not .Values.postgresql.auth.username.existingSecret }}
  username: {{ .Values.postgresql.auth.username.value | b64enc | quote }}
  {{- end }}
  {{- if not .Values.postgresql.auth.password.existingSecret }}
  password: {{ .Values.postgresql.auth.password.value | b64enc | quote }}
  {{- end }}
{{- end }}