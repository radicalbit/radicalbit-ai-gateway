suite: test psql-auth-secret.yaml manifest
templates:
  - commons/psql-auth-secret.yaml
tests:
  - it: should not create the manifest if not needed
    set:
      postgresql:
        auth:
          username:
            existingSecret: "secret"
          password:
            existingSecret: "secret"
    asserts:
      - hasDocuments:
          count: 0
  - it: should create the manifest if username is provided
    set:
      postgresql:
        auth:
          username:
            value: "admin"
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - isAPIVersion:
          of: v1
      - isNotEmpty:
          path: data.username
  - it: should create the manifest if password is provided
    set:
      postgresql:
        auth:
          password:
            value: "admin"
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: Secret
      - isAPIVersion:
          of: v1
      - isNotEmpty:
          path: data.password