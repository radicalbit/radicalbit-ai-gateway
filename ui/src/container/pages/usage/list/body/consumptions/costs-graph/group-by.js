const GROUP_BY = {
  groups: { key: 'groups', label: 'Groups' },
  credentials: { key: 'keys', label: 'Credentials' },
  models: { key: 'models', label: 'Models' },
};

const DEFAULT_GROUP_BY = GROUP_BY.groups.key;

export { GROUP_BY, DEFAULT_GROUP_BY };
