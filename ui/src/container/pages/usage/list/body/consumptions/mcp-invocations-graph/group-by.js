const MCP_GROUP_BY = {
  groups: { key: 'groups', label: 'Groups' },
  credentials: { key: 'keys', label: 'Credentials' },
  services: { key: 'services', label: 'MCP Services' },
};

const MCP_DEFAULT_GROUP_BY = MCP_GROUP_BY.services.key;

export { MCP_GROUP_BY, MCP_DEFAULT_GROUP_BY };
